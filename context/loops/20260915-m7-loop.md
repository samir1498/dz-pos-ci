---
title: 'M7 loop — paired phone proven, no manual test until green'
slug: 'm7-loop'
status: 'done'
category: 'loops'
created: 20260915
tldr: 'Built T4-T7 in a loop (atomic claim, device gate, phone session, idempotency, mobile cleanup, Maestro proof, closing sweep). Loop closed 2026-09-15, manual test green.'
plan: 'm7-paired-phone-proven'
---
# M7 loop

Samir, 2026-09-15: don't test until the loop says green. The laptop's dev
servers (`tmux dz: api/dev/mobile`) are up for scaffolding, not for a
verdict — T4 is still in review, the sign-in switcher just landed, and
the phone flow needs T5/T6.

The loop builds the rest as one branch after another, each with `dz-review`
and a `just ci` mirror check before merge. One session per machine, so no
worktree fan-out; the shared `.cargo-target` (~32G) and the WSL `C:` gate
(13G free) are why. Heavy builds go through `just` only.

## In this loop

- T4: one sale per retry key, queue bound to signer + print on retry
  (PR #89, core 5/5 incl. race, api 5/5, mobile 11/11, migration 54/54).
  Vite `import.meta.dirname` and sign-in `LanguageSwitcher` ride this PR.
- T5: mobile auth cleanup — bearer misuse already gone, tokens stored;
  harden `App.tsx`/`Till` headers, decide SecureStore vs AsyncStorage,
  no new API.
- T6: Maestro `pair-and-sell` on a real phone over Tailscale
  (pair → sign-in → sell → kill Wi-Fi → retry → one sale + print →
  revoke → 401). Phone is Expo Go at `exp://192.168.0.134:8081`, runner is
  `fedora` at `100.111.55.62`.
- T7: closing sweep — `docs/features.md §6`, `docs/architecture.md`
  transport table, `docs/roadmap.md` M7 demo, `dz-review` (money+roles),
  and the manual test roadmap.

## Not in this loop

- R6 (native Arabic words) and R8 (comptable). Human-held.
- Landing publish: still blocked on price + native Arabic.
- Windows certificate / updater key: Samir's holds.
- Report Astro migration: parked (`report-site-astro-migration`, paused).

## How the laptop is driven

`ssh -o BatchMode=yes -o ConnectTimeout=10 samir@100.111.55.62`, tmux
session `dz` (`api` / `dev` / `mobile`) and `setup` for one-offs. Repo
path `~/Developer/dz-pos`, report at `~/.dz-night/report`. `tmux ls`
is the health check; `just api` / `just dev` / `npx expo start` are the
three dev servers. Never `pkill -f`.

## Closed 2026-09-15

T4 PR #89, T5 PR #90, T6 PR #91, T7 PR #92 all merged to `main`. Laptop
pulled to `ba04c07`, `tmux dz` api/dev/mobile all 200, sign-in switcher
live, Vite warning gone. Manual test is now green — see the manual test
roadmap in `context/plans/archived/20260915-m7-paired-phone-proven.md`
(T7) for the matrix (loopback vs LAN, each role, offline/online, revoke
mid-shift, QR race, idempotency).
