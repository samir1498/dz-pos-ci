// Owned by retail alone (C7): `app-clinic/` carries no file at this path,
// so a clinic build's router root never registers `/till` at all. See
// `screens/signed-in/Till.tsx`.

export { default } from "../../screens/signed-in/Till";
